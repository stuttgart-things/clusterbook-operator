---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterbookloadbalancers.clusterbook.stuttgart-things.com
spec:
  group: clusterbook.stuttgart-things.com
  names:
    kind: ClusterbookLoadBalancer
    listKind: ClusterbookLoadBalancerList
    plural: clusterbookloadbalancers
    shortNames:
    - cblb
    singular: clusterbookloadbalancer
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.networkKey
      name: Network
      type: string
    - jsonPath: .status.ip
      name: IP
      type: string
    - jsonPath: .status.fqdn
      name: FQDN
      type: string
    - jsonPath: .status.poolName
      name: Pool
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              ciliumPool:
                description: |-
                  CiliumPool creates a CiliumLoadBalancerIPPool pinned to the reserved
                  IP. Exactly one target mode must be set.
                properties:
                  namespace:
                    description: |-
                      Namespace for the CiliumLoadBalancerIPPool. CiliumLoadBalancerIPPool
                      is actually cluster-scoped in Cilium; this field is kept empty.
                    type: string
                  poolName:
                    description: |-
                      PoolName is the name of the CiliumLoadBalancerIPPool to create.
                      Defaults to "<spec.name>-pool".
                    type: string
                  serviceSelector:
                    description: |-
                      ServiceSelector is copied verbatim onto the IP pool's
                      spec.serviceSelector. Services matching this selector get an IP
                      from the pool.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              createDNS:
                description: |-
                  CreateDNS asks clusterbook to create a wildcard DNS record for the
                  reserved IP. Useful when the IP backs an Ingress or Gateway API
                  frontend — the DNS is managed by clusterbook's PowerDNS integration.
                type: boolean
              name:
                description: Name is the reservation key under which clusterbook tracks
                  this LB IP.
                type: string
              networkKey:
                description: NetworkKey is the clusterbook network pool (e.g. "10.31.101").
                type: string
              providerConfigRef:
                description: |-
                  ProviderConfigRef points to a ClusterbookProviderConfig with the
                  clusterbook API URL and TLS options.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              releaseOnDelete:
                description: ReleaseOnDelete releases the clusterbook IP when the
                  CR is deleted.
                type: boolean
            required:
            - name
            - networkKey
            - providerConfigRef
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              fqdn:
                description: FQDN is the FQDN returned by clusterbook (only when createDNS=true).
                type: string
              ip:
                description: IP is the IP reserved in clusterbook and written to the
                  CiliumPool.
                type: string
              poolName:
                description: |-
                  PoolName is the name of the CiliumLoadBalancerIPPool created by
                  the operator.
                type: string
              zone:
                description: Zone is the DNS zone returned by clusterbook.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
