---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterbookproviderconfigs.clusterbook.stuttgart-things.com
spec:
  group: clusterbook.stuttgart-things.com
  names:
    kind: ClusterbookProviderConfig
    listKind: ClusterbookProviderConfigList
    plural: clusterbookproviderconfigs
    singular: clusterbookproviderconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              apiURL:
                type: string
              customCASecretRef:
                properties:
                  key:
                    description: Key defaults to "kubeconfig".
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              insecureSkipVerify:
                type: boolean
            required:
            - apiURL
            type: object
        type: object
    served: true
    storage: true
